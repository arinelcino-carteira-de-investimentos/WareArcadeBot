
# WareArcadeBot

Estrutura inicial profissional para um bot do Telegram.

## Instalação

```bash
pip install -r requirements.txt
```

Copie `.env.example` para `.env` e configure `TELEGRAM_BOT_TOKEN`.

Execute:

```bash
python app.py
```
